spring:
  cloud:
    nacos:
      discovery:
        server-addr: https://nacos.5houzi.com/
        register-enabled: false
        namespace: @nacos.config.namespace@
  profiles:
    active: @env@
logging:
  config: classpath:logback-spring.xml