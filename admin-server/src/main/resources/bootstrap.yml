spring:
  application:
    name: admin-service
  cloud:
    nacos:
      config:
        server-addr: nacos.5houzi.com
        file-extension: yaml
        namespace: @nacos.config.namespace@
dubbo:
  registry:
    address: spring-cloud://localhost
  cloud:
    subscribed-services: redis-service
