<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vhouzi.abc.admin.promote.mapper.PromoteMemberCardMapper" >
  <!--基本的sql查询字段 公共引用...-->
  <sql id="Base_Column_List">
    id, no, name, type, cover, price, expires, ext1, ext2, status, create_time,
    create_user, extention, content
  </sql>

  <!-- 基本的sql查询条件公共引用 -->
  <sql id="searchBy">
    <trim prefix="where" prefixOverrides="and|or">
      <if test="type !=null">
        and type = #{type}
      </if>
      <if test="status !=null">
        and status = #{status}
      </if>
    </trim>
  </sql>

  <select id="findSelective" resultMap="BaseResultMap" parameterType="java.util.HashMap">
    select
    <include refid="Base_Column_List"/>
    from member_tm_member_card
    <include refid="searchBy"/>
  </select>
</mapper>