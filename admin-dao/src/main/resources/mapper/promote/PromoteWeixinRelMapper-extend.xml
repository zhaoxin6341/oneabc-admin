<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vhouzi.abc.admin.promote.mapper.PromoteWeixinRelMapper" >
  <!--基本的sql查询字段 公共引用...-->
  <sql id="Base_Column_List">
    member_id, open_member_id, openid, nickname, sex, province, city, country,
    headimgurl, privilege, unionid, subscribe_time, groupid, tagid_list, subscribe_scene,
    qr_scene, qr_scene_str, mobile
  </sql>

  <!-- 基本的sql查询条件公共引用 -->
  <sql id="searchBy">
    <trim prefix="where" prefixOverrides="and|or">
      <if test="openid !=null and openid != ''">
        and openid = #{openid,jdbcType=VARCHAR}
      </if>
      <if test="memberId != null">
        and member_id = #{memberId,jdbcType=BIGINT}
      </if>
      <if test="mobile != null and mobile != ''">
        and mobile = #{mobile, jdbcType=VARCHAR}
      </if>
    </trim>
  </sql>

  <select id="findSelective" resultMap="BaseResultMap" parameterType="java.util.HashMap">
    select
    <include refid="Base_Column_List"/>
    from member_tr_weixin
    <include refid="searchBy"/>
  </select>

  <update id="updateSelective" parameterType="java.util.HashMap">
    update member_tr_weixin
    <trim prefix="set" suffixOverrides=",">
      <if test="openMemberId != null">
        open_member_id = #{openMemberId,jdbcType=BIGINT},
      </if>
      <if test="memberId != null">
        member_id = #{memberId,jdbcType=BIGINT},
      </if>
      <if test="tagId != null and tagId != ''">
        tagid_list = #{tagIds},
      </if>
    </trim>
    where open_id=#{openId}
  </update>
</mapper>