<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vhouzi.abc.admin.notification.mapper.SmsTplMapper" >
  <!--基本的sql查询字段 公共引用...-->
  <sql id="Base_Column_List">
    id, type, type_name, tpl, status, create_time, update_time
  </sql>

  <!-- 基本的sql查询条件公共引用 -->
  <sql id="searchBy">
    <trim prefix="where" prefixOverrides="and|or">
      <if test="type !=null and type != ''">
        and type = #{type,jdbcType=VARCHAR}
      </if>
      <if test="status !=null">
        and status = #{status,jdbcType=INTEGER}
      </if>
      <if test="typeName != null and typeName != ''">
        and type_name = #{typeName, jdbcType=VARCHAR}
      </if>
    </trim>
  </sql>

  <select id="findSelective" resultMap="BaseResultMap" parameterType="java.util.HashMap">
    select
    <include refid="Base_Column_List"/>
    from notify_tm_sms_tpl
    <include refid="searchBy"/>
  </select>

  <select id="listByObj" parameterType="SmsTpl" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List"/>
    from notify_tm_sms_tpl
    <include refid="searchBy"/>
  </select>

  <delete id="deleteSysSmsTemplateByIds" parameterType="String">
    delete from notify_tm_sms_tpl where id in
    <foreach item="id" collection="array" open="(" separator="," close=")">
      #{id}
    </foreach>
  </delete>
</mapper>